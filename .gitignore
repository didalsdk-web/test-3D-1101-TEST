# 환경 변수 파일 (민감한 정보 포함 가능)
# env/config.js 파일은 실제 환경에 따라 다를 수 있으므로 Git에서 제외
env/config.js

# 로컬 개발 환경 파일
.env.local
*.local.js
env.local/

# 기타 민감한 정보
*.env
.env

# Firebase 서비스 계정 키 (절대 Git에 올리지 마세요!)
env/serviceAccountKey.json
serviceAccountKey.json

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# JSON 파일 (package.json 제외)
*.json
!package.json
!package-lock.json
!tsconfig.json
!vercel.json

